<h1>Food Entries</h1>

<% @food_entries.group("DATE_TRUNC('month', entry_date)").each do |fe| %>
  <div class="card">
    <div class="card-body">
      <div class="card-title"><%= fe.entry_date %></div>
    </div>
  </div>
<% end %>