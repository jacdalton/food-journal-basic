<h1>Food Entries</h1>

<% @entries.each do |fe| %>
  <div class="card">
    <div class="card-body">
      <div class="card-title"><%= fe.entry_date %></div>
      <% fe.foods.each do |food| %>
        <div class="card-content"><%= food.name %></div>
      <% end %>
    </div>
  </div>
<% end %>